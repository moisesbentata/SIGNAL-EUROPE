# Instagram accounts to watch for new posts (best-effort, unofficial —
# see README.md for the caveats). Only caption text is read, never the
# source's images.
accounts:
  - scaling.europe
  - vesting
